import React from 'react';

export default class Menu extends React.Component{
    render(){
        return(
            <div id="skin-elements">

            </div>
        )
    }
}

//{this.props.textures.currentLoadedTextures.map((element, key) => this.skinElement(element, key))}