import React, {Component, PropTypes} from 'react'

export default class Settings extends Component {
    render() {
        return(
            <div>
                Settings
            </div>
        )
    }
}
